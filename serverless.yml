service: budget-master-test-api

provider:
  name: aws
  runtime: nodejs22.x
  stage: production
  region: us-east-1
  logRetentionInDays: 1
  timeout: 15
  stackName: budget-master-test-api
  apiName: budget-master-test-api

functions:
  api:
    handler: dist/lambda.handler
    events:
      - http:
          method: ANY
          path: /
      - http:
          method: ANY
          path: '{proxy+}'

plugins:
  - serverless-jetpack
  - serverless-offline